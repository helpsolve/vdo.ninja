// custom/trigger-utils.js - Future expansion (e.g., WS listener for director triggers)

// Example: Listen for custom WS messages (hook later if needed)
// window.addEventListener('message', e => {
//   if (e.data?.action === 'stinger') playStinger(e.data.index);
// });

console.log("Trigger utils loaded");
